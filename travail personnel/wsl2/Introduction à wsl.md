wsl ou wsl.exe c'est le même
wsl -l : lister les distribution linux installer
wsl -l -v : pour plus de détail sur les distribution installer
wsl --shutdown : stopper tous les distributions en cours d'exécution
